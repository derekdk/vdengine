# Quick smoke test — shorter for fast CI validation
# Verifies example launches and exits cleanly
wait startup
# Keep a modest post-startup settle delay to reduce startup flakiness in CI.
wait 3000
exit
