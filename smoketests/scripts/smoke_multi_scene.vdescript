# Smoke test for multi-scene demo
# Tests scene switching and new diagnostics
wait startup
wait 1000

# Verify initial state
assert rendered_scene_count >= 1
assert scenes_created >= 1

# Cycle through scenes (if keys are bound)
press 1
wait 500
press 2
wait 500
press 3
wait 500
press 4
wait 500

wait 2s
exit
