#pragma once

/**
 * @file Defaults.h
 * @brief Default constants shared across VDE API components
 */

namespace vde::defaults {

inline constexpr float DEFAULT_SCREEN_WIDTH = 1920.0f;
inline constexpr float DEFAULT_SCREEN_HEIGHT = 1080.0f;

}  // namespace vde::defaults
